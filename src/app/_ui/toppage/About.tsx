import Section from "./components/Section";

const About = () => (
  <Section title="About">
    <p>Webエンジニアです。</p>
    <p>主にLaravel、React(TypeScript)を書いています。</p>
  </Section>
);

export default About;
